Portfolio Lens PWA

このフォルダ一式をHTTPS対応のWebサーバーへ配置してください。
公開後、iPhoneのSafariでURLを開き、共有ボタン →「ホーム画面に追加」→「追加」でインストールできます。

重要: index.htmlだけを直接開く file:// 方式ではService Worker/PWAインストールは動作しません。
データはブラウザのlocalStorageに保存されます。定期的にアプリ内のバックアップ保存を利用してください。
